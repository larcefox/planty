# 🌱 Planty — Visual Gantt Editor for PlantUML

Монорепозиторий Planty с разведёнными фронтендом и бэкендом. Цель — визуальный онлайн-редактор диаграмм Ганта в формате PlantUML с AI-помощником.

## 📂 Структура
```
planty/
  frontend/           # Vite + React + TypeScript + TailwindCSS
    src/components/   # AppLayout и placeholder-компоненты
  backend/            # FastAPI backend
    app/api/          # Маршруты (health)
    app/models/       # Доменные модели (пустые заглушки)
    app/services/     # Бизнес-логика (пустые заглушки)
```

## 🚀 Быстрый старт
### Требования
- Node.js 18+
- Python 3.11+

### Установка зависимостей
```bash
# фронтенд
cd frontend
npm install

# бэкенд
cd backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### Dev-режим
Запустите фронтенд и бэкенд одной командой из корня:
```bash
npm run dev
```
Отдельно:
```bash
npm run dev:frontend    # http://localhost:5173
npm run dev:backend     # http://localhost:8000
```

### Проверка API
```
GET http://localhost:8000/api/health
{
  "status": "ok"
}
```
CORS открыт для `http://localhost:5173`.

### Линтинг
```bash
npm run lint:frontend   # ESLint + Prettier (frontend)
```

## ✨ Что уже есть
- Стартовый React layout: шапка, две колонки с плейсхолдерами (Gantt, PlantUML editor, AI assistant), футер.
- TailwindCSS + базовая цветовая схема Planty.
- Минимальный FastAPI с `/api/health` и настроенным CORS.
- Корневые скрипты dev/линт для удобного запуска.

## 🗺 Roadmap (MVP)
- Эпик 1: базовая инфраструктура (текущий статус — готово).
- Эпик 2: канвас диаграммы Ганта и редактор PlantUML.
- Эпик 3: AI-помощник и расширенные интеграции.
