# 🌱 Planty — Visual Gantt Editor for PlantUML

**Planty** — это первый визуальный онлайн-редактор диаграмм Ганта в формате **PlantUML Gantt**, объединяющий:

* интерактивный drag-and-drop Gantt редактор,
* двустороннюю синхронизацию с PlantUML-кодом,
* встроенного **AI-помощника** для генерации, редактирования и анализа диаграмм.

Planty позволяет работать с диаграммами так же удобно, как в GanttPRO / Monday / MS Project — но с преимуществами текстового формата PlantUML.

---

## ✨ Возможности (MVP)

### 🔄 Двусторонняя синхронизация

Любые визуальные изменения сразу обновляют PlantUML-код, и наоборот.

### 🎨 Интерактивный Gantt-редактор

* Перетаскивание задач по времени (drag).
* Изменение длительности задач (resize).
* Перемещение задач по порядку (drag-n-drop).
* Создание, удаление, редактирование задач и вех.
* Группировка задач (summary tasks).
* Зависимости (Finish-to-Start).
* Панель свойств задачи.

### 📝 PlantUML Editor

* Редактор кода (Monaco).
* Подсветка синтаксиса и ошибок.
* Импорт/экспорт `.puml`.
* Сравнение и синхронизация изменений.

### 🤖 AI-помощник Planty

* Генерация диаграммы по текстовому описанию.
* Правка PlantUML-кода с объяснением ошибок.
* Модификация проекта по запросу пользователя.
* Автоматический анализ зависимостей и структуры.
* Предпросмотр изменений от ИИ.

### 🛠 Дополнительно

* Undo/Redo для всех операций.
* Производительная работа до 200+ задач.
* Лёгкая архитектура для расширения.

---

## 🧱 Технологии

### Frontend

* **Vite + React + TypeScript**
* **TailwindCSS**
* Zustand (состояние)
* Monaco Editor
* Собственный Gantt Canvas (HTML + pointer events)

### Backend

* **Python + FastAPI**
* ASGI сервер: uvicorn
* AI-proxy endpoint для работы с LLM

---

## 📂 Структура проекта

```
planty/
  frontend/           # Vite + React + TS
    src/
      components/
      domain/
      plantuml/
      store/
      ai/
  backend/            # FastAPI на Python
    app/
      api/
      services/
      models/
```

---

## 🚀 Запуск проекта

### Frontend

```bash
cd frontend
npm install
npm run dev
```

### Backend

```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

Фронтенд обращается к API по адресу:

```
http://localhost:8000/api
```

---

## 🤝 Вклад в проект

Мы открыты к предложениям и пул-реквестам:

* улучшение UX диаграммы,
* новые AI-фичи,
* оптимизационные патчи,
* расширение парсера PlantUML.

---

## 📜 Лицензия

MIT License — свободно используйте, модифицируйте и встраивайте Planty в свои проекты.

---

## ⭐️ Поддержите проект

Если вам нравится идея — поставьте ⭐ на GitHub, это сильно помогает в развитии проекта!